package com.guet.liang.stockchat.ui.settings

/** Shared cross-platform type; this declaration defines a stable contract for callers. */
internal data class SettingsSegmentOption<T>(val value: T, val label: String)
