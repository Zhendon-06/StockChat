# 2026-08-24

- 按 WorkBuddy 新会话页风格优化聊天首页（`shared/.../ui/StockChatPage.kt`）：
  - WelcomeContent：标题改为「MiMo，我帮你看行情」（logo 已含 StockChat 字标，避免重复），logo 改用 resizeContain，尺寸上调（welcomeLogoWidth 120-140，见 StockChatLayoutMetrics.kt）。
  - 新增横向滑动快捷建议卡 SuggestionCardRow/SuggestionCard（白底胶囊 + boxShadow + assets 图标），点击直接 sendMessage；建议项用 data/level/ranking/table 四张图标。
  - Composer 面板：圆角 34/26 → 30/24，边框 borderStrong → border，加柔和 boxShadow。
  - 新增文件级 `StockChatSuggestion` data class；import BoxShadow。
  - 验证：`:shared:compileKotlinJs` 与 `:shared:compileDebugKotlinAndroid` 均通过。
- 二次调整（用户自己改了顶栏：新建/更多按钮 pill + hamburger，阴影 BoxShadow(1,5,14,0x1A000000)）：
  - Composer 面板阴影改为与顶部按钮一致 (1,5,14,0x1A)，并移除 overflow(true)（会裁剪阴影、留下发黑底边）。
  - 折叠态占位文字垂直居中：TextArea top 15dp → 22.5dp（=(68-23)/2；Android TextArea 文字从顶部绘制，实测偏高约 7.3dp）。
  - 注意：SuggestionCardRow 里 Scroller 的 forEach 内容后来被删空了，建议卡当前不渲染；恢复时建议改用普通 View 横排（横向 Scroller 在此版本未渲染出内容）。
- 输入框跟随键盘：改为「键盘顶起输入框」式同步动画。Kuikly `keyboardHeightChange` 的 `KeyboardParams` 自带 `duration`（秒；Android 端硬编码 0.2，iOS 取系统键盘动画时长）。做法：StockChatPage 新增 `keyboardAnimDuration` observable（默认 0.2f），回调里先记 duration 再更新 keyboardHeight；消息列表容器和 ComposerDock 的 `animate(Animation.easeInOut(0.22f), ctx.keyboardHeight)` 均改为 `Animation.easeInOut(ctx.keyboardAnimDuration)`。验证：`:shared:compileDebugKotlinAndroid` 通过。
