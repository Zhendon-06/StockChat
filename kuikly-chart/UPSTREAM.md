# Source provenance

Source: https://github.com/Zhendon-06/KuiklyChartView
Local development checkout: sibling `KuiklyChartView` repository, `kuikly-chart` module.

This copy contains the finance additions developed in that checkout on 2026-09-08.
Changes are local and have not been published to GitHub or Maven.
Synchronize using `tools/sync-chart-component.sh`; edit reusable chart behavior upstream first.
Build definitions use this host project's `Version` so the component follows StockChat's Kuikly/Kotlin versions.
